# 需要 Python 3.10 以上（anthropic 1.x 與 python-dotenv 都要求 >=3.10）
# 建議 3.12：discord.py 2.7 官方標到 3.12 為止
discord.py>=2.4.0
anthropic>=1.0.0
python-dotenv>=1.0.1
# 時區資料庫：Windows 沒有，Debian slim 容器裡也沒有。少了它 zoneinfo 找不到 Asia/Taipei
tzdata>=2024.1
# Python 3.13 把 audioop 移出標準庫，但 discord/player.py 仍然無條件 import 它。
# 少了這個，3.13+ 連 `import discord` 都會 ModuleNotFoundError。
audioop-lts>=0.2.1 ; python_version >= "3.13"
